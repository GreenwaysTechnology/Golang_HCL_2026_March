Install Buf CLI in windows
To install the Buf CLI on Windows, you can use a package manager like WinGet or Scoop, or manually download the binary. 
Option 1: Using WinGet (Recommended)
WinGet is built into Windows 10 and 11. Run this in your terminal:
powershell
winget install bufbuild.buf